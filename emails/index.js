function sendSignAgreementEmail(creatorEmail, signeeEmail, link) {
  console.log(`Sending sign agreement email to ${signeeEmail} from ${creatorEmail} with link ${link}`);
}

module.exports = {
    sendSignAgreementEmail
};